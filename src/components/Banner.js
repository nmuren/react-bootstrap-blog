import React from "react";

const Banner = () => {
  return (
    <button type="button" className="my-5 css-banner">
      <div>
        <span>BANNER ADS</span>
      </div>
    </button>
  );
};

export default Banner;
